// criado um array que existem propriedades, tambem chamado de atributos. E nessas propriedade temos os valores.

const usuario = {
    nome: "Mateus",
    idade: 25,
    casado: false
}
console.log(usuario)
console.log(usuario.casado) // apresentar na tela somente valor da minha propriedade casado.

/* 

    usuario → é uma variável.
    nome, idade e casado → são propriedades do objeto.
    "Mateus", 25 e false → são os valores dessas propriedades.

*/